# 更新日志

本文件记录用户可感知的变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.0] - 2026-08-01

### 新增

- **检查更新**：有新版本时会提示。「轻图」菜单里可手动检查，也可关掉启动时的自动检查。
  这是本 App 唯一的网络请求，只读取 GitHub 上公开的版本号，不发送任何图片或使用数据
- **Homebrew 分发**：`brew install --cask syanbo/tap/minim`，之后随 `brew upgrade` 更新

### 修复

- DMG 文件名改用 ASCII（`Minim-x.y.z.dmg`）。GitHub 会过滤附件名里的非 ASCII 字符，
  `轻图-1.0.0.dmg` 曾被截成 `-1.0.0.dmg`

## [1.0.0] - 2026-08-01

首个公开版本。项目由「智图 Next」改名而来并以 MIT 协议开源，版本号从 1.0.0 重新起算。

### 新增

- **静态 WebP 输入**：此前只支持动图 WebP，静态 WebP 拖进来会被静默丢弃
- **三个转换开关**（WebP / JPG / PNG），语义统一为「额外输出一份该格式，不替换主输出」。
  目标格式与输入相同时自动跳过，动图一律不适用
- 命令行新增 `--png`，`--convert` 更名为 `--jpg`

### 修复

以下问题均由「静态 WebP 成为合法输入格式」引入，其中三条会造成数据丢失：

- **转换产物不再覆盖源文件**。此前 `logo.png` 的 WebP 候选路径与用户自己的 `logo.webp`
  相同，会先删除再写入。现在命中冲突会跳过并在界面与命令行提示
- **动图 WebP 不再被压成单帧**。改为直接读取 WebP 容器的 VP8X 动画标志位，
  不再依赖 ImageIO 的帧数判断
- **静态 WebP 增加代际劣化保护**：重压后节省不足 10% 时保留原图。WebP 没有可解析的
  质量元数据，反复重编码会持续劣化
- **「无损 PNG」名副其实**：转 PNG 候选此前会跟随全局质量档跑 pngquant 量化到 256 色，
  与界面和文档的「无损」承诺相反。现在固定走无损链路
- **转换失败不再静默**：此前编码错误被吞掉，任务显示「完成」但输出目录里没有文件
- **取消操作生效**：此前取消后候选文件仍会落盘，任务翻回「已完成」
- **命令行未知选项直接报错**，不再被当作图片路径导致「不支持的图片格式」

[1.1.0]: https://github.com/syanbo/Minim/releases/tag/v1.1.0
[1.0.0]: https://github.com/syanbo/Minim/releases/tag/v1.0.0
